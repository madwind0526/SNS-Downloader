# Copy this file to .env and fill in values
# .env is gitignored — never commit it

# Access token for Render (cloud) mode.
# Leave empty or unset to disable auth (local PC use).
# Set this in Render dashboard → Environment → SECRET_FILES or Environment Variables.
ACCESS_TOKEN=

# Optional: override download directory
# DOWNLOADS_DIR=C:\Users\yourname\Downloads
